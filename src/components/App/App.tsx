import { Container } from '@mui/material';
import React from 'react';
import { AppContent } from '../AppContent';

const App: React.FC = () => {
  return (
    <Container>
      <AppContent />
    </Container>
  );
}

export default App;
